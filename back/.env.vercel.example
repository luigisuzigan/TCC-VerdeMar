# Variáveis de Ambiente para Vercel
# Copie estes valores para a aba "Environment Variables" no painel da Vercel

# JWT Secret (use um valor aleatório forte)
JWT_SECRET=your-super-secret-jwt-key-here-minimum-32-characters

# Admin inicial
ADMIN_EMAIL=admin@verdemarsc.com
ADMIN_PASSWORD=admin123

# Database MySQL (AlwaysData ou outro)
DATABASE_URL=mysql://user:password@host/database?connection_limit=5&pool_timeout=30&connect_timeout=30

# Shadow Database (para migrations - pode ser o mesmo DB com sufixo _shadow)
SHADOW_DATABASE_URL=mysql://user:password@host/database_shadow?connection_limit=2&pool_timeout=30&connect_timeout=30

# Google Maps API Key
GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here

# CORS - URL do frontend na Vercel
ALLOWED_ORIGIN=https://tcc-verde-mar.vercel.app

# Node Environment (Vercel já seta isso automaticamente)
NODE_ENV=production

# Debug Prisma (opcional - só ative se precisar debugar queries)
# DEBUG_PRISMA=true
